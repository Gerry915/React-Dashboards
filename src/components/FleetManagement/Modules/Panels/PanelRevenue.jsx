import styles from "style/FleetManagement/FleetMain.module.scss"

const PanelRevenue = () => {
  return (
    <div className={`${styles['main-panel']} ${styles['main-revenue']}`}>Revenue</div>
  )
}

export default PanelRevenue