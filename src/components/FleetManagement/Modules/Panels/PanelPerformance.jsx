import styles from "style/FleetManagement/FleetMain.module.scss";

const PanelPerformance = () => {
  return (
    <div
      className={`${styles["main-panel"]} ${styles["main-performance"]}`}
    >Performance</div>
  );
};

export default PanelPerformance;
