import styles from "style/FleetManagement/FleetMain.module.scss"

const PanelEnv = () => {
  return (
    <div className={`${styles['main-panel']} ${styles['main-env']}`}>CO2</div>
  )
}

export default PanelEnv